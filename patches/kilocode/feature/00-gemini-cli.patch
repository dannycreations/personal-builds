file packages/opencode/src/kilocode/provider/provider.ts

 // This module exports patch functions and data that the upstream provider.ts
 // calls at well-defined injection points (each marked with kilocode_change).
 
+import { createGeminiCli } from "@/provider/sdk/gemini-cli"
 import { createKilo, type KiloProvider, AI_SDK_PROVIDERS, PROMPTS } from "@kilocode/kilo-gateway"
 import { DEFAULT_HEADERS } from "@/kilocode/const"
 import { AiSdkProvider, Prompt } from "@/provider/models"

label export const REQUEST_TIMEOUT_MS = 120_000 // 2 minutes

 type BundledSDK = { languageModel(modelId: string): LanguageModelV3 }
 
 export const KILO_BUNDLED_PROVIDERS: Record<string, () => Promise<(options: any) => BundledSDK>> = {
+  "gemini-cli": async () => createGeminiCli,
   "@kilocode/kilo-gateway": async () => createKilo as unknown as (options: any) => BundledSDK,
 }
 

file packages/opencode/src/provider/models.ts

+import { GEMINI_CLI_MODELS } from '@/provider/sdk/gemini-cli'
 import { Global } from "../global"
 import { Log } from "../util"
 import path from "path"

label export async function get() {

   // kilocode_change start
   const providers = result as Record<string, Provider>
 
+  if (!providers["gemini-cli"]) {
+    providers["gemini-cli"] = GEMINI_CLI_MODELS
+  }
+
   if (providers["kilo"]) {
     delete providers["kilo"]
   }

file packages/opencode/src/provider/provider.ts
label function useLanguageModel(sdk: any) {

 
 function custom(dep: CustomDep): Record<string, CustomLoader> {
   return {
+    "gemini-cli": Effect.fnUntraced(function* () {
+      const config = yield* dep.config()
+      const providerConfig = config.provider?.["gemini-cli"]
+      const options = providerConfig?.options ?? {}
+      return { autoload: true, options }
+    }),
     anthropic: () =>
       Effect.succeed({
         autoload: false,

file packages/opencode/src/provider/sdk/gemini-cli/gemini-cli.ts

+import {
+  type LanguageModelV3,
+  type LanguageModelV3CallOptions,
+  type LanguageModelV3GenerateResult,
+  type LanguageModelV3Prompt,
+  type LanguageModelV3ToolChoice,
+} from "@ai-sdk/provider"
+import {
+  combineHeaders,
+  createEventSourceResponseHandler,
+  createJsonResponseHandler,
+  generateId,
+  postJsonToApi,
+  type FetchFunction,
+  type ResponseHandler,
+} from "@ai-sdk/provider-utils"
+import { APICallError } from "ai"
+import { arch, homedir, platform } from "node:os"
+import { dirname, join } from "node:path"
+import { z } from "zod/v4"
+import { InstallationVersion } from "../../../installation/version"
+
+const geminiErrorSchema = z.object({
+  error: z.object({
+    code: z.number().optional(),
+    message: z.string().optional(),
+    status: z.string().optional(),
+    details: z
+      .array(
+        z.object({
+          "@type": z.string().optional(),
+          reason: z.string().optional(),
+          domain: z.string().optional(),
+          metadata: z.record(z.string(), z.string()).optional(),
+        }),
+      )
+      .optional(),
+  }),
+})
+
+const OAUTH_CLIENT_ID = "681255809395-oo8ft2oprdrnp9e3aqf6av3hmdib135j.apps.googleusercontent.com"
+const OAUTH_CLIENT_SECRET = "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"
+const OAUTH_DEFAULT_PATH = join(homedir(), ".gemini", "oauth_creds.json")
+
+interface OAuthCredentials {
+  readonly access_token: string
+  readonly refresh_token: string
+  readonly token_type: string
+  readonly expiry_date: number
+}
+
+export interface GeminiCliProviderConfig {
+  readonly baseURL?: string
+  readonly geminiCliOAuthPath?: string
+  readonly geminiCliProjectId?: string
+  readonly fetch?: FetchFunction
+}
+
+export class GeminiCliProvider implements LanguageModelV3 {
+  public readonly specificationVersion = "v3"
+  public readonly provider = "gemini-cli"
+  public readonly modelId: string
+  public readonly supportedUrls = {}
+
+  private readonly config: GeminiCliProviderConfig
+
+  private credPath: string = OAUTH_DEFAULT_PATH
+  private credentials?: OAuthCredentials
+  private projectId?: string
+  private turnCount = 0
+
+  public constructor(modelId: string, config: GeminiCliProviderConfig) {
+    this.modelId = modelId
+    this.config = config
+  }
+
+  private failedResponseHandler: ResponseHandler<APICallError> = async ({ response, url, requestBodyValues }) => {
+    const value = await response.json()
+    const parsed = geminiErrorSchema.safeParse(value)
+    const error = parsed.success ? parsed.data.error : undefined
+
+    const isRetryable =
+      response.status === 429 ||
+      response.status >= 500 ||
+      error?.status === "RESOURCE_EXHAUSTED" ||
+      error?.details?.some((d) => d.reason === "RATE_LIMIT_EXCEEDED")
+
+    return {
+      value: new APICallError({
+        message: error?.message || response.statusText,
+        url,
+        requestBodyValues,
+        statusCode: response.status,
+        responseBody: JSON.stringify(value),
+        isRetryable: !!isRetryable,
+      }),
+      responseHeaders: Object.fromEntries(response.headers.entries()),
+    }
+  }
+
+  public async doGenerate(
+    options: LanguageModelV3CallOptions,
+  ): Promise<Awaited<ReturnType<LanguageModelV3["doGenerate"]>>> {
+    const { headers, body } = await this.prepareRequest(options)
+
+    const {
+      value: responseBody,
+      responseHeaders,
+      rawValue,
+    } = await postJsonToApi({
+      url: `${this.config.baseURL}:generateContent`,
+      headers,
+      body,
+      failedResponseHandler: this.failedResponseHandler,
+      successfulResponseHandler: createJsonResponseHandler(z.any()),
+      abortSignal: options.abortSignal,
+      fetch: this.config.fetch,
+    })
+
+    const response = responseBody.response
+    const candidate = response.candidates?.[0]
+    const usage = response.usageMetadata
+
+    return {
+      content:
+        candidate?.content?.parts
+          ?.filter((p: any) => p.text && !p.thought)
+          ?.map((p: any) => p.text)
+          ?.join("") || "",
+      finishReason: this.mapFinishReason(candidate?.finishReason),
+      usage: {
+        inputTokens: usage?.promptTokenCount ?? 0,
+        outputTokens: usage?.candidatesTokenCount ?? 0,
+      },
+      response: { headers: responseHeaders, body: JSON.stringify(rawValue) },
+      warnings: [],
+    }
+  }
+
+  public async doStream(
+    options: LanguageModelV3CallOptions,
+  ): Promise<Awaited<ReturnType<LanguageModelV3["doStream"]>>> {
+    const { headers, body } = await this.prepareRequest(options)
+
+    const { value: stream, responseHeaders } = await postJsonToApi({
+      url: `${this.config.baseURL}:streamGenerateContent?alt=sse`,
+      headers,
+      body,
+      failedResponseHandler: this.failedResponseHandler,
+      successfulResponseHandler: createEventSourceResponseHandler(z.any()),
+      abortSignal: options.abortSignal,
+      fetch: this.config.fetch,
+    })
+
+    let finishReason: LanguageModelV3GenerateResult["finishReason"] = { unified: "other", raw: undefined }
+    let usage: any = null
+    let activeReasoning: string | null = null
+    let activeText: string | null = null
+    let activeToolCall: string | null = null
+
+    const self = this
+    return {
+      stream: stream.pipeThrough(
+        new TransformStream({
+          start(controller) {
+            controller.enqueue({ type: "stream-start", warnings: [] })
+          },
+          transform(chunk, controller) {
+            if (!chunk.success) {
+              controller.enqueue({ type: "error", error: chunk.error })
+              return
+            }
+
+            const response = chunk.value.response
+            const candidate = response.candidates?.[0]
+
+            if (candidate?.content?.parts) {
+              for (const part of candidate.content.parts) {
+                if (part.text) {
+                  if (part.thought) {
+                    if (!activeReasoning) {
+                      activeReasoning = generateId()
+                      controller.enqueue({ type: "reasoning-start", id: activeReasoning })
+                    }
+                    controller.enqueue({ type: "reasoning-delta", id: activeReasoning, delta: part.text })
+                  } else {
+                    if (!activeText) {
+                      activeText = generateId()
+                      controller.enqueue({ type: "text-start", id: activeText })
+                    }
+                    controller.enqueue({ type: "text-delta", id: activeText, delta: part.text })
+                  }
+                } else if (part.functionCall) {
+                  activeToolCall = generateId()
+                  const toolName = part.functionCall.name
+                  const input = JSON.stringify(part.functionCall.args)
+
+                  controller.enqueue({
+                    type: "tool-input-start",
+                    id: activeToolCall,
+                    toolName,
+                  })
+
+                  controller.enqueue({
+                    type: "tool-call",
+                    toolCallId: activeToolCall,
+                    toolName,
+                    input,
+                    providerMetadata: part.thoughtSignature
+                      ? { [self.provider]: { thoughtSignature: part.thoughtSignature } }
+                      : undefined,
+                  })
+                }
+              }
+            }
+
+            if (response.usageMetadata) usage = response.usageMetadata
+            if (candidate?.finishReason) {
+              finishReason = self.mapFinishReason(candidate.finishReason, activeToolCall)
+            }
+          },
+          flush(controller) {
+            if (activeReasoning) {
+              controller.enqueue({ type: "reasoning-end", id: activeReasoning })
+              activeReasoning = null
+            }
+            if (activeText) {
+              controller.enqueue({ type: "text-end", id: activeText })
+              activeText = null
+            }
+            if (activeToolCall) {
+              controller.enqueue({ type: "tool-input-end", id: activeToolCall })
+              activeToolCall = null
+            }
+
+            controller.enqueue({
+              type: "finish",
+              finishReason,
+              usage: {
+                inputTokens: usage?.promptTokenCount ?? 0,
+                outputTokens: usage?.candidatesTokenCount ?? 0,
+              },
+            })
+          },
+        }),
+      ),
+      response: { headers: responseHeaders },
+    }
+  }
+
+  private mapFinishReason(
+    reason: string | undefined,
+    toolCall?: string | null,
+  ): LanguageModelV3GenerateResult["finishReason"] {
+    if (toolCall) return { unified: "tool-calls", raw: undefined }
+    if (!reason) return { unified: "other", raw: undefined }
+
+    let unified: LanguageModelV3GenerateResult["finishReason"]["unified"] = "other"
+    if (reason === "STOP") unified = "stop"
+    else if (reason === "MAX_TOKENS") unified = "length"
+    else if (reason === "SAFETY") unified = "content-filter"
+    else if (reason === "RECITATION") unified = "content-filter"
+    else if (reason === "OTHER") unified = "other"
+    return { unified, raw: reason }
+  }
+
+  private async ensureAuthenticated(): Promise<void> {
+    if (!this.credentials) {
+      let geminiCliOAuthPath = this.config.geminiCliOAuthPath || process.env.GOOGLE_OAUTH_PATH
+      if (!geminiCliOAuthPath) {
+        try {
+          const content = await Bun.file(join(dirname(OAUTH_DEFAULT_PATH), ".env")).text()
+          const match = content.match(/GOOGLE_OAUTH_PATH=["']?([^"'\r\n\s;]+)["']?/)
+          const oauthPath = match ? match[1].trim() : ""
+          if (oauthPath) geminiCliOAuthPath = oauthPath
+        } catch {}
+      }
+
+      this.credPath = geminiCliOAuthPath || OAUTH_DEFAULT_PATH
+
+      const file = Bun.file(this.credPath)
+      if (await file.exists()) {
+        this.credentials = await file.json()
+      }
+    }
+
+    if (this.credentials && this.credentials.expiry_date - 60_000 < Date.now()) {
+      const { value: responseBody } = await postJsonToApi({
+        url: "https://oauth2.googleapis.com/token",
+        body: {
+          client_id: OAUTH_CLIENT_ID,
+          client_secret: OAUTH_CLIENT_SECRET,
+          refresh_token: this.credentials.refresh_token,
+          grant_type: "refresh_token",
+        },
+        failedResponseHandler: this.failedResponseHandler,
+        successfulResponseHandler: createJsonResponseHandler(
+          z.object({
+            access_token: z.string(),
+            refresh_token: z.string().optional(),
+            token_type: z.string().optional(),
+            expires_in: z.number().optional(),
+          }),
+        ),
+        fetch: this.config.fetch,
+      })
+
+      this.credentials = {
+        access_token: responseBody.access_token,
+        refresh_token: responseBody.refresh_token || this.credentials.refresh_token,
+        token_type: responseBody.token_type || "Bearer",
+        expiry_date: responseBody.expires_in ? Date.now() + responseBody.expires_in * 1000 : Date.now() + 3600000,
+      }
+      await Bun.write(this.credPath, JSON.stringify(this.credentials, null, 2))
+    }
+  }
+
+  private async discoverProjectId(): Promise<void> {
+    if (this.projectId) return
+
+    if (this.config.geminiCliProjectId) {
+      this.projectId = this.config.geminiCliProjectId
+      return
+    }
+
+    const projectId = process.env.GOOGLE_CLOUD_PROJECT
+    if (projectId) {
+      this.projectId = projectId
+      return
+    }
+
+    try {
+      const content = await Bun.file(join(dirname(this.credPath), ".env")).text()
+      const match = content.match(/GOOGLE_CLOUD_PROJECT=["']?([^"'\r\n\s;]+)["']?/)
+      const projectId = match ? match[1].trim() : ""
+      if (projectId) {
+        this.projectId = projectId
+        return
+      }
+    } catch {}
+
+    const metadata = { ideType: "IDE_UNSPECIFIED", platform: "PLATFORM_UNSPECIFIED", pluginType: "GEMINI" }
+    const load = await this.callEndpoint("loadCodeAssist", {
+      cloudaicompanionProject: this.projectId,
+      metadata: { ...metadata, duetProject: this.projectId },
+    })
+
+    if (load.cloudaicompanionProject) {
+      this.projectId = load.cloudaicompanionProject
+      return
+    }
+
+    const tierId = load.allowedTiers?.find((t: any) => t.isDefault)?.id || "free-tier"
+    let onboard = await this.callEndpoint("onboardUser", { tierId, cloudaicompanionProject: undefined, metadata })
+
+    if (!onboard.done && onboard.name) {
+      const op = onboard.name
+      const fetchFn = this.config.fetch || fetch
+      while (!onboard.done) {
+        await new Promise((f) => setTimeout(f, 5000))
+        const response = await fetchFn(`${this.config.baseURL}/${op}`, {
+          method: "GET",
+          headers: {
+            Authorization: `Bearer ${this.credentials?.access_token}`,
+            "Content-Type": "application/json",
+          },
+        })
+        onboard = await response.json()
+      }
+    }
+    this.projectId = onboard.response?.cloudaicompanionProject?.id
+  }
+
+  private async callEndpoint(method: string, body: any, retry = true): Promise<any> {
+    try {
+      await this.ensureAuthenticated()
+
+      const { value: responseBody } = await postJsonToApi({
+        url: `${this.config.baseURL}:${method}`,
+        headers: { Authorization: `Bearer ${this.credentials?.access_token}` },
+        body,
+        failedResponseHandler: this.failedResponseHandler,
+        successfulResponseHandler: createJsonResponseHandler(z.any()),
+        fetch: this.config.fetch,
+      })
+
+      return responseBody
+    } catch (error: any) {
+      if (error.code === 401 && retry) {
+        this.credentials = undefined
+        return this.callEndpoint(method, body, false)
+      }
+      throw error
+    }
+  }
+
+  private async prepareRequest(options: LanguageModelV3CallOptions) {
+    await this.ensureAuthenticated()
+    await this.discoverProjectId()
+
+    const geminiOptions = options.providerOptions?.[this.provider]
+    const sessionId = geminiOptions?.session_id
+    const userPromptId = sessionId ? `${sessionId}########${++this.turnCount}` : undefined
+    const { systemInstruction, contents } = this.mapPromptToGemini(options.prompt)
+    const toolSettings = this.mapToolsToGemini(options.tools, options.toolChoice)
+
+    const body = {
+      model: this.modelId,
+      project: this.projectId,
+      user_prompt_id: userPromptId,
+      request: {
+        systemInstruction,
+        contents,
+        ...(toolSettings?.tools ? { tools: toolSettings.tools } : {}),
+        ...(toolSettings?.toolConfig ? { toolConfig: toolSettings.toolConfig } : {}),
+        generationConfig: {
+          ...(options.temperature != null ? { temperature: options.temperature } : {}),
+          ...(options.topP != null ? { topP: options.topP } : {}),
+          ...(options.topK != null ? { topK: options.topK } : {}),
+          ...(options.maxOutputTokens != null ? { maxOutputTokens: options.maxOutputTokens } : {}),
+          ...(geminiOptions?.thinkingConfig ? { thinkingConfig: geminiOptions.thinkingConfig } : {}),
+        },
+        ...(sessionId ? { session_id: sessionId } : {}),
+      },
+    }
+
+    const headers = combineHeaders(
+      {
+        Authorization: `Bearer ${this.credentials?.access_token}`,
+        "User-Agent": `GeminiCLI/${InstallationVersion}/${this.modelId} (${platform()}; ${arch()})`,
+      },
+      options.headers,
+    )
+
+    return { headers, body }
+  }
+
+  private mapPromptToGemini(prompt: LanguageModelV3Prompt) {
+    let systemInstruction: any = undefined
+    const contents: any[] = []
+
+    for (const message of prompt) {
+      const role = message.role === "assistant" ? "model" : "user"
+      const parts: any[] = []
+
+      for (const part of message.content) {
+        if (typeof part === "string") {
+          parts.push({ text: part })
+          continue
+        }
+
+        switch (part.type) {
+          case "text": {
+            if (message.role === "system") {
+              systemInstruction = {
+                role: "user",
+                parts: [{ text: part.text }],
+              }
+              continue
+            }
+            parts.push({ text: part.text })
+            break
+          }
+          case "file": {
+            parts.push({
+              inlineData: {
+                mimeType: part.mediaType,
+                data: part.data,
+              },
+            })
+            break
+          }
+          case "tool-call": {
+            const thoughtSignature = part.providerOptions?.[this.provider]?.thoughtSignature
+            parts.push({
+              functionCall: {
+                name: part.toolName,
+                args: part.input,
+              },
+              thoughtSignature: thoughtSignature ?? "skip_thought_signature_validator",
+            })
+            break
+          }
+          case "tool-result": {
+            parts.push({
+              functionResponse: {
+                id: part.toolCallId,
+                name: part.toolName,
+                response: { output: (part as any).output.value },
+              },
+            })
+            break
+          }
+        }
+      }
+
+      if (parts.length > 0) {
+        contents.push({ role, parts })
+      }
+    }
+
+    return { systemInstruction, contents }
+  }
+
+  private mapToolsToGemini(tools?: LanguageModelV3CallOptions["tools"], toolChoice?: LanguageModelV3ToolChoice) {
+    if (!tools || tools.length === 0) return {}
+
+    const functionDeclarations = tools
+      .map((tool) => {
+        if (tool.type === "function") {
+          return {
+            name: tool.name,
+            description: tool.description ?? "",
+            parametersJsonSchema: tool.inputSchema,
+          }
+        }
+        return undefined
+      })
+      .filter(Boolean)
+
+    const mappedTools = [{ functionDeclarations }]
+
+    let toolConfig: any = undefined
+    if (toolChoice) {
+      if (toolChoice.type === "auto") {
+        toolConfig = { functionCallingConfig: { mode: "AUTO" } }
+      } else if (toolChoice.type === "required") {
+        toolConfig = { functionCallingConfig: { mode: "ANY" } }
+      } else if (toolChoice.type === "tool") {
+        toolConfig = {
+          functionCallingConfig: {
+            mode: "ANY",
+            allowedFunctionNames: [toolChoice.toolName],
+          },
+        }
+      }
+    }
+
+    return { tools: mappedTools, toolConfig }
+  }
+}

file packages/opencode/src/provider/sdk/gemini-cli/index.ts

+import { withoutTrailingSlash } from "@ai-sdk/provider-utils"
+import { NoSuchModelError } from "ai"
+import { GeminiCliProvider, type GeminiCliProviderConfig } from "./gemini-cli"
+
+export const GEMINI_CLI_MODELS = {
+  id: "gemini-cli",
+  npm: "gemini-cli",
+  name: "Gemini CLI",
+  env: ["GOOGLE_OAUTH_PATH", "GOOGLE_CLOUD_PROJECT"],
+  api: "https://cloudcode-pa.googleapis.com/v1internal",
+  models: {
+    "gemini-3.1-pro-preview": {
+      id: "gemini-3.1-pro-preview",
+      name: "Gemini 3.1 Pro Preview",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 65_536,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+    "gemini-3.1-flash-lite-preview": {
+      id: "gemini-3.1-flash-lite-preview",
+      name: "Gemini 3 Flash Lite Preview",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 65_536,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+    "gemini-3-pro-preview": {
+      id: "gemini-3-pro-preview",
+      name: "Gemini 3 Pro Preview",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 65_536,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+    "gemini-3-flash-preview": {
+      id: "gemini-3-flash-preview",
+      name: "Gemini 3 Flash Preview",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 65_536,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+    "gemini-2.5-pro": {
+      id: "gemini-2.5-pro",
+      name: "Gemini 2.5 Pro",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 64_000,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+    "gemini-2.5-flash": {
+      id: "gemini-2.5-flash",
+      name: "Gemini 2.5 Flash",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 64_000,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+    "gemini-2.5-flash-lite": {
+      id: "gemini-2.5-flash-lite",
+      name: "Gemini 2.5 Flash Lite",
+      release_date: "2025-02-25",
+      attachment: true,
+      reasoning: true,
+      temperature: true,
+      tool_call: true,
+      limit: {
+        context: 1_048_576,
+        output: 64_000,
+      },
+      modalities: {
+        input: ["text", "image"],
+        output: ["text"],
+      },
+    },
+  },
+} as const
+
+export function createGeminiCli(options: GeminiCliProviderConfig = {}) {
+  const createModel = (modelId: string) => {
+    return new GeminiCliProvider(modelId, {
+      baseURL: withoutTrailingSlash(options.baseURL) as string,
+      geminiCliOAuthPath: options.geminiCliOAuthPath,
+      geminiCliProjectId: options.geminiCliProjectId,
+      fetch: options.fetch,
+    })
+  }
+
+  const provider = (modelId: string) => createModel(modelId)
+
+  provider.languageModel = createModel
+  provider.embeddingModel = (modelId: string) => {
+    throw new NoSuchModelError({ modelId, modelType: "embeddingModel" })
+  }
+  provider.imageModel = (modelId: string) => {
+    throw new NoSuchModelError({ modelId, modelType: "imageModel" })
+  }
+
+  return provider
+}

file packages/opencode/src/provider/transform.ts
label export const OUTPUT_TOKEN_MAX = Flag.KILO_EXPERIMENTAL_OUTPUT_TOKEN_MAX || 32_00

 // Maps npm package to the key the AI SDK expects for providerOptions
 function sdkKey(npm: string): string | undefined {
   switch (npm) {
+    case "gemini-cli":
+      return "gemini-cli"
     case "@ai-sdk/github-copilot":
       return "copilot"
     case "@ai-sdk/azure":

label export function options(input: {

 }): Record<string, any> {
   const result: Record<string, any> = {}
 
+  if (input.model.api.npm === "gemini-cli") {
+    result["session_id"] = input.sessionID
+  }
+
   // openai and providers using openai package should set store to false by default.
   if (
     input.model.providerID === "openai" ||

label export function options(input: {

     result["promptCacheKey"] = input.sessionID
   }
 
-  if (input.model.api.npm === "@ai-sdk/google" || input.model.api.npm === "@ai-sdk/google-vertex") {
+  if (
+    input.model.api.npm === "gemini-cli" ||
+    input.model.api.npm === "@ai-sdk/google" ||
+    input.model.api.npm === "@ai-sdk/google-vertex"
+  ) {
     if (input.model.capabilities.reasoning) {
       result["thinkingConfig"] = {
         includeThoughts: true,
       }
       if (input.model.api.id.includes("gemini-3")) {
         result["thinkingConfig"]["thinkingLevel"] = "high"
+      } else if (input.model.api.id.includes("gemini-2")) {
+        result["thinkingConfig"]["thinkingBudget"] = 24576
       }
     }
   }
