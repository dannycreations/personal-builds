file src/api/providers/anthropic.ts
label export class AnthropicHandler extends BaseProvider implements SingleCompletionHa

 			throw error
 		}
 
-		const content = message.content.find(({ type }) => type === "text")
-		return content?.type === "text" ? content.text : ""
+    const item = message.content.find(c => c.type === "text" || c.type === "thinking");
+    return item?.text || item?.thinking || "";
 	}
 
 	// kilocode_change start
